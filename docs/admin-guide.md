# Admin Guide

Login as bootstrap admin, change the password, create users with temporary strong passwords, review audit log, configure safe global limits in environment/settings, and delete users when access should be revoked. Do not store secrets, resumes, databases, uploads, or run artifacts in Git.
