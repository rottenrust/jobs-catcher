# Criteria Schema

Canonical format is JSON with `schema_version`, `name`, `profile_summary`, `search`, `scoring`, and `output`. JSON/YAML imports are validated and saved as immutable versions. `scoring.max_score` is 22 and thresholds must descend to `Мимо` at 0.
